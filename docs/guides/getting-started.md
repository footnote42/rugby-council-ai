# Getting Started

1. Install dependencies: `pip install -r requirements.txt`
2. Configure `.env` values for LM Studio API base URL and keys
3. Run: `python council.py` to start a session orchestration
