# Configuration

`config.py` contains the main configuration values for the council process, including model names, temperatures, and token limits.

Edit `config.py` or set environment variables to customize behaviour.